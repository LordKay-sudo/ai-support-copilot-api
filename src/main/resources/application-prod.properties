spring.config.activate.on-profile=prod

# Debug retrieval is off in production unless you explicitly opt in.
copilot.retrieval.debug-search-enabled=false

spring.security.oauth2.resourceserver.jwt.secret-key=${COPILOT_JWT_SECRET}
