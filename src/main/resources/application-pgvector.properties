copilot.retrieval.store-type=pgvector

spring.datasource.url=${COPILOT_DB_URL:jdbc:postgresql://localhost:5432/copilot}
spring.datasource.username=${COPILOT_DB_USERNAME:postgres}
spring.datasource.password=${COPILOT_DB_PASSWORD:postgres}
