# Ensure prod profile tests see the same retrieval gating as packaged prod config
# (test classpath application.properties must not leave debug search enabled).
copilot.retrieval.debug-search-enabled=false
